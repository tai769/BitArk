server:
  port: 8080  # 节点1端口

spring:
  application:
    name: read-adapter-node1  # 应用名称

# 日志配置
logging:
  level:
    root: INFO
    adapter: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"